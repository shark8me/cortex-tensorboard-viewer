(ns cortex-tensorboard-viewer.core-test
  (:require [clojure.test :refer :all]
            [cortex-tensorboard-viewer.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
